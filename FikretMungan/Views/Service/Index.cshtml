@model IEnumerable<Service>

@{
    ViewData["Title"] = "Hizmetlerimiz";
    ViewData["MetaDescription"] = "İdeio Creative Reklam Ajansı";
    ViewData["MetaKeywords"] = "İdeio Creative Reklam Ajansı";
}

<div class="template-content">

    <div class="template-content-section template-padding-top-reset template-padding-bottom-5 template-main">

        <div class="template-component-header-subheader">
            <h2>Hizmetlerimiz</h2>
            <h6>Yenidoğan takibinden uyku eğitimine, sağlıklı çocuk izleminden online danışmanlığa kadar geniş bir yelpaze.</h6>
            <div></div>
        </div>

        <div class="template-layout-50x50 template-clear-fix">

            @{
                int sayac = 0; // 1. Sayaç tanımlıyoruz
            }

            @foreach (var item in Model)
            {
                // 2. Sıra çiftse (0, 2, 4...) SOLA, tekse (1, 3, 5...) SAĞA yaslıyoruz
                string columnClass = (sayac % 2 == 0) ? "template-layout-column-left" : "template-layout-column-right";

                // 3. Alt alta yapışmamaları için margin ekliyoruz (reset'i kaldırdım veya margin ekledim)
                // "template-margin-bottom-reset" yerine "template-margin-bottom-30" gibi bir şey gerekebilir.
                // Ancak temanın orijinalinde reset varsa ve otomatik boşluk bırakıyorsa kalsın.
                // Ben şimdilik hizalama sorunu çözdüm:

                <div class="@columnClass template-margin-bottom-reset" style="margin-bottom:30px !important">
                    <div class="template-component-image template-component-image-hover-slide-enable">

                        <img src="/img/@item.Image" alt="@item.Title" />
                        <span><span><span></span></span></span>

                        <div>
                            <h5>@item.Title</h5>
                        </div>
                        <div class="template-align-center template-margin-top-2">
                            <a asp-controller="Service" asp-action="Detail" asp-route-slug="@item.Slug" class="template-component-button template-component-button-style-1">Daha Fazla<i></i></a>
                        </div>
                    </div>
                </div>

                sayac++; // 4. Sayacı her turda 1 artırıyoruz
            }

        </div>

    </div>
</div>